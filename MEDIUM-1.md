# MEDIUM-1:位级操作
## 模块一
1. 211（十进制）=11010011（二进制）
2. 10110110（二进制）=182（十进制）
3. 先将十进制转化为二进制，看二进制的最后一位，如果是1即为奇数，如果是0即为偶数
## 模块二
1. 
   1. - rest1=8，&的运算逻辑为先将两个十进制数转化为二进制，然后两个数对齐，然后每一位进行比较，全1为1，有0则0，因此运算后的结果为0000 1000，换算为十进制即为8
      - rest2=29，|的运算逻辑为有1则1，全0则0，因此运算后的结果为0001 1101，换算为十进制则为25
      - rest3=21，^的运算逻辑是不同为1，相同为0，因此运算后的结果是0001 0101，换算为十进制为21
      - rest4=60，a<<的意思是让a的二进制，左移两位高位的0去掉，低位补0，最终运算的结果是0011 1100，换算为十进制即为602.
   2. ```c
      #include <stdio.h>
      int main(){
      int x,n;
      printf("input x,n:");
      scanf("%d %d",&x,&n);
      int result = (x >> (n - 1)) & 1;
      result%=2;
      printf("output:%d",result);
      return 0;
      }
      ```
   3. ```c
      #include <stdio.h>
      int abc(int x,int n,int t)
      {
      x=x&~(1<<(n-1));
      x=x|(t<<(n-1));
      return x;
      }

      int main()
      {
      int x,n,t;
      scanf("%d %d %d",&x,&n,&t);
      int result=abc(x,n,t);
      printf("%d",result);
      return 0;
      }
      ```
   4. ```c
      #include <stdio.h>

      int main()
      {
      int x;
      int result;
      scanf("%d",&x);
      result=x&-x;
      printf("%d",result);

      return 0;
      }
      ```
  
   5. ```c
      #include <stdio.h>/*核心思路，用数组来模仿二进制*/
      #include <string.h>

      #define MAX 150/*设置数组的盒子数，一定要大于题目要求的100*/

      int main(void) {
      char a[MAX], b[MAX];
      scanf("%s %s", a, b);/*输入*/

      int la = strlen(a);
      int lb = strlen(b);
      int maxL;   
      if (la>lb){
        maxL=la;
      }else{
        maxL=lb;
      }/*找到a，b两个数组中较长的那一个*/

      char pa[MAX], pb[MAX];
      for (int i = 0; i < maxL - la; i++) pa[i] = '0';
      for (int i = 0; i < la; i++)        pa[maxL - la + i] = a[i];
      pa[maxL] = '\0';

      for (int i = 0; i < maxL - lb; i++) pb[i] = '0';
      for (int i = 0; i < lb; i++)        pb[maxL - lb + i] = b[i];
      pb[maxL] = '\0';/*对a，b中较短的那一个进行高位补零处理，只有位数相同才方便进行位运算*/

      int p = 0;
      while (p < maxL && pa[p] == pb[p]) p++;/*找到高位中a，b相同的部分*/

      char res[MAX];
      for (int i = 0; i < p; i++)        res[i] = pa[i];
      for (int i = p; i < maxL; i++)     res[i] = '0';
      res[maxL] = '\0';/*前p位照抄公共前缀，后面的部分全部补0*/

      int start = 0;
      while (res[start] == '0' && res[start + 1] != '\0') start++;
      printf("%s\n", res + start);/*去掉结果前面的 0 再打印（全是 0 时至少留一个*/

      return 0;
      }

      ```  
   >为什么这样运算可以实现最终的目标：首先我们找出公共前缀，这说明[a,b]区间内所有的整数都满足这个公共前缀，所以在整个区间进行&的过程中，这部分公共前缀会被一直保留下去，而当我们找到第一个不相同的数的时候，这个数及其后面剩下的数运算到最后一定0（&全1则1，有0则0）
2. 
     1. 
   - 运算符不同，逻辑运算：&&，||，！，位运算（&，|，~，^）
   - 运算对象不同，逻辑运算对象是整个变量或表达式，将其视为真或假，位运算的对象是bit
   - 返回值不同，逻辑运算返回1/0，位运算操作后得到一个新的整数
   1. 
    - 基本运算律：交换律，结合律，分配律
    - 德摩根定律：~(a&b)=~a|~b,~(a|b)=~a&~b
    - 移位运算性质，左移相当于乘二的幂，右移相当于除以二的幂
    - 掩码高级应用性质：
   ```c

   x&~(1<<n)，将第n位清零
   x|(1<<n),将第n位置1
   x&-x，提取最低位的1
   x&（x-1），消除最低位的1
   ```
   - 算术右移与逻辑右移的区别：逻辑右移：高位统一补0，适用于无符号数，算术右移，高位补符号位，适用于有符号数
   - 
## 模块三
1. 
- 第一步先构建一个函数int hasCommonChar，这个函数的作用是利用mask1，mask2的二进制，每次识别一个字母就可以找出其对应的位置，然后将这个位置上的0变成1，因为|的作用是有1则1，因此不论有多少个相同字母被识别到，对应位置都只会有1个1，然后返回时&的作用是有0则0，因此只有同样为1（也就是出现过相同字母的位置）才会被保留下来
- 主函数中只用输入字符，然后再判断有没有相同位即可，有则是1，无则是0
## 模块四
**N皇后问题**：
1. 定义两个变量，ans用来记录答案的数量，limit作为边界掩码，起到两个作用，**屏蔽高位和皇后放置在棋盘之外的情况**，**作为放置结束的条件，当出现row=limit的情况时，说明放置完成，这时情况数要+1**
2. 设置dfs函数来确定终止条件，row代表列数，ld代表主对角线的攻击情况，而rd代表右对角线的攻击情况
3. 定义变量available来代表可以放置的区域，`int available = limit & ~(row | ld | rd)`;available等于limit（棋盘范围），和~(row | ld | rd)（还可以放置的区域的交集）
4. 使用while循环，根据前面取最低位的1的题目可知，`int p = available & -available;`  就是将最低位的1赋值给p变量，然后每找到一个最低位的1就可以用 `dfs(row | p, (ld | p) << 1, (rd | p) >> 1);`递归的形式将这种情况给记录下来

>limit = (1 << n) - 1; 代表的是limit这个掩码初始状态为00000000，然后将一个1向左推n为然后-1，得到例如000111111的掩码，通过这个掩码就可以得到这个可放置区域的边界是5，同时通过&达到高位屏蔽的效果

## 模块五
**最短步数问题的解析**
>1，2两点为题目中已给出代码的解析，后面是我对我的代码的解析
1. 设置结构体，里面存放好棋盘的编号，和到达当前位置所用的步数dist
2. 设置一个函数来计算从start->target的最短步数
   - 首先先对墙的设置做一个判定，如果墙出现在了start，target的位置，那么直接不可达
   - （核心）bfs队列与访问位图，bfs（广度优先搜索），可以做到像水波一样从start位置开始一步步向target位置扩散，由于bfs满足逐层阔散，所以它可以保证每一次到达一个新的点的时候都可以做到是最短步数，在这里我们设置front和rear两个变量，front表示下一个要取出处理的节点，而rear表示下一个新节点所要放的位置。设置visited去表示所有已经去到过的点
   - 起点入队放置在start=0的位置，并且给它标记上已访问
   - 用dr，dc两个数组表示偏移量
3. 
   - 设置一个while循环，当`front<rear`时，当前位置等于front+1，由于front位于队首，而rear位于队尾，因此只要rear不出现在15的位置停下来，就会一直进行bfs的循环，去让front往前走
   - if的结束判定：当target=cur的时候说明已经走到了终点，这个时候直接返回当前步数
   - 设置两个变量r，c，` int r = cur.pos / 4;
        int c = cur.pos % 4;`r代表行数，而c代表列数
   - 设置一个for循环，每一次循环的时候都需要对行和列分别进行增加，和一次越界筛查，如果发现出现越界的情况，就直接跳过这个方向，然后更新位置，如果发现走到了墙，或者是已经访问过的地方同样跳过这次循环，最后标记新节点已访问，然后步数+1